# 対訳文庫 Design System

## Direction

`getdesign.md` の Notion design analysis を基調にした、静かなオンライン文庫のための翻案。
参照元が示す「warm minimalism / serif headings / soft surfaces」を採用し、SaaS 的な装飾は避ける。

目標は次の三点である。

1. 生成りの紙、墨色、細い罫線による落ち着いた編集デザイン
2. 検索・絞り込み・表示切替が迷わず使える現代的なUI
3. グラデーション、過剰なカード、絵文字、発光表現を使わず、AI生成サイトらしさを抑える

参考: https://getdesign.md/notion/design-md

## Color tokens

| Token | Light | Dark | Use |
|---|---:|---:|---|
| `--canvas` | `#f4f1ea` | `#171714` | ページ背景 |
| `--surface` | `#fbfaf6` | `#201f1b` | カード・操作面 |
| `--surface-strong` | `#ffffff` | `#282721` | 強調面 |
| `--ink` | `#1d1c19` | `#f0ede5` | 見出し・本文 |
| `--ink-secondary` | `#55514a` | `#bbb5aa` | 補助本文 |
| `--ink-muted` | `#7a756c` | `#8e897f` | メタ情報 |
| `--hairline` | `#dcd7ce` | `#3a3832` | 罫線 |
| `--accent` | `#315f75` | `#8bb8c8` | リンク・選択状態 |
| `--accent-strong` | `#23495b` | `#b1d4df` | ホバー・フォーカス |
| `--focus` | `#2b78a0` | `#8fc9e4` | キーボードフォーカス |

色は意味のある箇所だけに使う。装飾用の多色パレットは導入しない。

## Typography

- UI: system sans-serif。ナビゲーション、検索、ボタン、メタ情報に使用。
- 日本語本文・見出し: 游明朝、ヒラギノ明朝など端末内の明朝体。
- 欧文本文・原題: Iowan Old Style、Palatino、Georgia 系。
- 本文は `line-height: 1.9–2.0`。1行は日本語でおよそ30–42字を目安とする。
- 大見出しは太さより余白と字間で階層を作る。

## Spacing

8pxを基準とする。主要値は 4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96px。
カード内は24px、セクション間は64–96pxを基本とする。

## Radius and elevation

- 入力・小ボタン: 6–8px
- コンテンツ面: 12px
- ピルは検索結果件数など本当に丸形が適切な要素だけ
- 影は原則使わず、1pxの罫線で面を分ける
- 浮遊要素のみ、低不透明度の小さな影を許可する

## Components

### Header

半透明ではなく、紙面色の不透明な面と1px罫線。ロゴ、主要導線、テーマ切替だけを置く。

### Search console

検索入力、作家絞り込み、並び替えを一つの面にまとめる。入力は44px以上。
モバイルでは縦積みにする。検索結果はJavaScriptがなくても全件読める。

### Work entry

書影風のダミー画像は作らない。連番、和題、原題、作家、ジャンル、言語を編集目録のように並べる。
ホバーでは色と矢印の移動だけを使い、大きく浮かせない。

### Reader

本文が主役。デスクトップは対訳2列、モバイルは日本語→原文の縦積み。
操作バーは固定するが、本文を隠さない。読書進捗は1–2pxの線で示す。

### Advertising

広告枠は本文・操作UIと明確に区別し、「広告」と表示する。空枠は非表示。
モバイルで本文と重ならず、誤クリックを誘う配置にしない。

## Responsive behavior

- `< 720px`: 1列、操作部品は44px以上、対訳は縦積み
- `720–1023px`: 目録は1列、検索コントロールは2列まで
- `>= 1024px`: 目録2列、対訳2列、最大幅1200px
- `prefers-reduced-motion` では移動アニメーションを無効化

